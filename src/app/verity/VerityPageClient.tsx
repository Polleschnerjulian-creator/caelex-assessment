"use client";

/**
 * @deprecated This component is no longer used.
 * The Verity page is now a server component in page.tsx.
 * Kept temporarily to avoid breaking any stale imports.
 */
export default function VerityPageClient() {
  return null;
}
