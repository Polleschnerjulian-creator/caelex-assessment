// See /atlas/loading.tsx for the rationale — we deliberately skip the
// suspense fallback so navigation keeps the previous page visible.
export default function JurisdictionsLoading() {
  return null;
}
