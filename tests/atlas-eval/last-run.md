# Atlas SpaceLaw Bench — last run

_Placeholder. Run `npm run test:atlas-eval` with the required env
vars to populate this file:_

```bash
ATLAS_EVAL_USER_ID=<user-id-of-LAW_FIRM-org-member> \
ATLAS_EVAL_ORG_ID=<that-user's-org-id> \
ANTHROPIC_API_KEY=...   # or AI_GATEWAY_API_KEY
npm run test:atlas-eval
```

The runner will overwrite this file with a real markdown summary,
plus `tests/atlas-eval/last-run.json` with the full report consumed
by `/atlas/settings/eval`.
